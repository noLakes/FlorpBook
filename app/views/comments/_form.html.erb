<%= form_for( :comment , remote: true) do |f| %>
  <%= f.hidden_field :post_id, params[:post_id] %>
  <%= f.label "Your Comment:"%>
  </br>
  <%= f.text_area :body, size: "50x5" %>
  </br>
  <%= f.submit :create, name: nil%>
<% end %>